#!/usr/bin/env bash
# -----------------------------------------------------------------------------
#  ci/db-backup.sh — ยังไม่ได้เขียน (TODO ของ Phase 3)
#  Jenkinsfile ของ SIT/UAT/PROD เรียกสคริปต์นี้
#
#  สิ่งที่ต้องทำ: pg_dump ฐานข้อมูลของ environment ที่ระบุ (\$1)
#    - เก็บไฟล์พร้อม timestamp + คำนวณ checksum
#    - ถ้ามี --verify ให้ทดสอบ restore ลง DB ชั่วคราวแล้วเทียบจำนวนแถว
#    - ต้องผ่านก่อนจึงจะ migrate ได้ (ห้าม deploy โดยไม่มีจุดย้อนกลับ)
#  สคริปต์จงใจ exit 1 เพื่อไม่ให้ pipeline ผ่านไปทั้งที่ยังไม่ได้ทำงานจริง
# -----------------------------------------------------------------------------
set -euo pipefail
echo "[db-backup] ยังไม่ได้เขียนสคริปต์นี้ — ต้องทำใน Phase 3 ก่อนใช้กับข้อมูลจริง" >&2
exit 1
